name: optimize me 2
categories:
  - rev
value: 100
flag: bcactf{sl0w_m4th_f4st_c0d3}
description: |-
  just optimize me!
hints:
  - how can you speed up the binary? this time, you can't just randomly nop stuff, it's actually calculating the flag!
  - i helped you out and wrote some faster funciotns. too bad i forgot to use them
files:
  - src: /app/slow_flag
    dest: slow_flag
    container: static
authors:
  - Colin
visible: true
deploy:
  static:
    build: .
