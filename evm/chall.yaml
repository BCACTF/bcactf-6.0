name: Example Problem
categories:
  - misc
value: 200
flag: bcactf{5011d11y_r3v3r53_3n61n33r3d_600d_j08}
 
# Alternatively:
# flag:
#   file: ./flag.txt
# 
description: |-
  in the big 25', we only use the top languages for everything.
  i even signed a **contract** to do things, hopefully it has some interesting **functions** for you
  lol it might be a **flag checker**, have fun
hints:
  - what is evm? (vm = virtual machine)
files:
  - src: FlagChecker.evm
authors:
  - Colin
visible: true
# TODO: deployment
